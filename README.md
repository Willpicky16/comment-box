# My first React App

Welcome to my first React App called `Comment box` which allows you to view and post comments.

To get started follow the steps:

- Clone the app `git clone REPOSITORY_NAME`

- cd into the app `cd APP_NAME`

- install the packages `npm install`

- Now your ready to start the app `npm start`

Happy playing with the app! :D
